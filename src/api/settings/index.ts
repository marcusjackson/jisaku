/**
 * Settings API Module
 *
 * General application settings (if any app-wide config is needed).
 * Note: Classification types moved to api/classification/
 *       Position types moved to api/position/
 */

// Reserved for app-wide settings if needed
// export * from './app-settings-repository'
